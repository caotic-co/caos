version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  build-and-test:
    executor: python/default
    steps:
      - checkout
      - run:
          command: ./run_tests.py
          name: Unit tests

workflows:
  main:
    jobs:
      - build-and-test
